+ Create a Visual Studio project using the Console Application template.
- It is required: 
	+ Create the class named "AbstractHandler".
	+ Create the methods in the body of the class:
		- void Open() and Test method,
		- void Create() and Test method,
		- void Chenge() and Test method,
		- void Save() and Test method.
	- Create AbstractHandler‘s the derived classes XMLHandler, TXTHandler, DOCHandler 
- Write a program which will recognize a document; and there should be methods for each format to open, create, edit and to save the document in a certain format.